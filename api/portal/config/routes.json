{
  "routes": [
    {
      "method": "GET",
      "path": "/portals",
      "handler": "portal.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/portals/count",
      "handler": "portal.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/portals/:id",
      "handler": "portal.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/portals",
      "handler": "portal.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/portals/:id",
      "handler": "portal.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/portals/:id",
      "handler": "portal.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
